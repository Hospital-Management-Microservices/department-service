# Server Port
server.port=8084

# Application Name
spring.application.name=department-service

# Neon.tech PostgreSQL Database
spring.datasource.url=jdbc:postgresql://ep-odd-waterfall-a1juougx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_aECFtImR7h0N
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# Other Services URLs (through API Gateway)
doctor.service.url=http://localhost:8080/doctors
